name: Build + Publish

on:
  push:
  schedule:
    - cron: '0 4 * * *'

jobs:
  build:
    uses: ./.github/workflows/build.yml
  publish:
    uses: ./.github/workflows/publish.yml
    needs: [build]
    # secrets:
    #   npm_token: ${{ env.NPM_TOKEN }}
