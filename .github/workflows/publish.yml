name: Publish

on:
  # push:
  # schedule:
  #   - cron: '0 4 * * *'
  workflow_call:
    # secrets:
    #   npm_token:
    #     required: true

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  npm:
    runs-on: ubuntu-latest
    environment: npm
    steps:
      - uses: actions/checkout@v2
      - uses: actions/download-artifact@v2
      - run: ls -lisa prebuilds
