runs:
  using: composite
  steps:
    - uses: actions/checkout@v2
    - uses: ./.github/actions/install
    - run: yarn prebuild
      shell: bash
    - uses: actions/upload-artifact@v2
      with:
        name: prebuilds
        path: prebuilds/**/*
